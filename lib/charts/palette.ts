/**
 * Кольори серій графіків.
 *
 * Дублюють токени `--income` / `--expense` / `--net` з globals.css. Дубль
 * вимушений: recharts віддає колір як SVG-атрибут (`stroke="…"`), а в
 * атрибуті `var(--income)` не резолвиться — працює тільки в CSS-властивості.
 *
 * Відтінки -700 обрані так, щоб один колір годився і як заливка під білим
 * текстом, і як текст на білому. Контраст до білого: 5.48 / 6.47 / 7.10.
 * Попередні #10b981 / #ef4444 давали 2.54 / 3.76 — не проходили навіть
 * планку 3:1 для графіки, не кажучи про 4.5:1 для підписів.
 */
export const CHART_COLORS = {
  income: "#047857",
  expense: "#b91c1c",
  net: "#6d28d9",
} as const;

/**
 * Стиль підпису в легенді. Recharts за замовчуванням фарбує текст у колір
 * серії — саме це давало 1.92:1 на назвах категорій. Колір лишається в
 * маркері, підпис читається звичайним текстом.
 */
export const LEGEND_LABEL_STYLE: React.CSSProperties = {
  color: "var(--color-foreground)",
};
